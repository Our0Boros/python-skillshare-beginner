name = 'omer'.title()
date_today = 24
welcome_message = "Hi " + name + "\nWelcome back, today's date is the " + str(date_today) + " of October."

print(welcome_message)

#The use of str(int value) converts it integer into a string
#what is know as casting for C++

#If used without casting the compiler will tell it can't concatenate str and int
